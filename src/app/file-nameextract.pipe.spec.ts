import { FileNameextractPipe } from './file-nameextract.pipe';

describe('FileNameextractPipe', () => {
  it('create an instance', () => {
    const pipe = new FileNameextractPipe();
    expect(pipe).toBeTruthy();
  });
});
